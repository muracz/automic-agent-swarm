version: '2'
services:
    unix_alpine:
        #   build: agents/unix_alpine/.
        build: agents/unix_centos/.
        environment:
            - cp=192.168.99.100:2217
            - system=AUTOMIC
            - prefix=alpine
# For agents with smgr
#  unix_alpine:
#    build: agents/unix_alpine_w_smgr/.
#    environment:
#      - cp=localhost:2217
#      - system=AUTOMIC
#      - prefix=alpine
